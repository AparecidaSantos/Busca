= simple_form_for(@filial) do |f|
  = f.error_notification

  .form-inputs
    = f.input :nome 
    = f.input :endereco, label: "Endereço"
    = f.input :cnpj 
    = f.association :empresa, label_method: :nome

  .form-actions
    = f.submit "Salvar"
